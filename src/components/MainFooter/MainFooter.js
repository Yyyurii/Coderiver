import UnresolvedTickets from '../UnresolvedTickets';
import './MainFooter';

const MainFooter = () => {
  return (
    <div className="main-footer">
      <UnresolvedTickets />
    </div>
  );
};

export default MainFooter;